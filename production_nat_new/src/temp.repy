

def cb(rip, rport, sock, th, lh):
  pass


def server():
  h = waitforconn('128.1.2.3', 12345, cb)


def client():
  sock = openconn('127.1.2.3', 12345)
  sock.send('a')
  sock.recv(1)
  sock.close()



if callfunc == 'initialize':
  try:
    i = 1/0
  except:
    print getlasterror()

