
DEFAULT_SHIM_STACK_NAMES = '(ClientServerShim,[CoordinationShim],[CoordinationShim][ReverseConn][NAT])'

include layermain.repy
include nat_obj.repy
include TCPReverseConn.repy
include ClientServerShim.repy



def print_it(rip,rport,sock,th,lh):
  while True:
   print sock.recv(1024)


if callfunc == 'initialize':
  serverkey = 'DIRECTSERVER'
  
  handle = layered_waitforconn(serverkey,12345,print_it)
    
